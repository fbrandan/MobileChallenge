<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Button
        android:id="@+id/btnAddToCart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="add to\ncart"
        />

    <TextView
        android:id="@+id/tvItemRowCode"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="#someCode"
        android:textColor="@color/gray"
        android:gravity="center_horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toLeftOf="@id/btnAddToCart"
        app:layout_constraintLeft_toLeftOf="parent"
        />
    <TextView
        android:id="@+id/tvItemRowName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="someName"
        android:textColor="@color/black"
        app:layout_constraintTop_toBottomOf="@id/tvItemRowCode"
        app:layout_constraintRight_toLeftOf="@id/btnAddToCart"
        app:layout_constraintLeft_toLeftOf="parent"
        />
    <TextView
        android:id="@+id/tvItemRowPrice"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="$0.0"
        android:textColor="@color/cyan"
        android:gravity="right"
        app:layout_constraintTop_toBottomOf="@id/tvItemRowName"
        app:layout_constraintRight_toLeftOf="@id/btnAddToCart"
        app:layout_constraintLeft_toLeftOf="parent"
        />


</androidx.constraintlayout.widget.ConstraintLayout>